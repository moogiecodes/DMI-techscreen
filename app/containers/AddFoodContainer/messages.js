/*
 * AddFoodContainer Messages
 *
 * This contains all the text for the AddFoodContainer container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddFoodContainer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddFoodContainer container!',
  },
});
